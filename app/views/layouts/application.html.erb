<!DOCTYPE html PUBLIC "-//W3C//DTD XHTML 1.0 Transitional//EN" "http://www.w3.org/TR/xhtml1/DTD/xhtml1-transitional.dtd">
<html xmlns="http://www.w3.org/1999/xhtml">
<head>
<meta http-equiv="Content-Type" content="text/html; charset=iso-8859-1" />
  <title>ABTT - <% @cur_member ? @cur_member.full_name : 'Login' %></title>
  <%= stylesheet_link_tag "main.css" %>
  <%= javascript_include_tag :defaults %>
</head>
<body>
  <div class="header">
    <%= environment_notice -%>
    <%= link_to "<img src=\"/images/header.png\" style=\"border: 0 solid black;\" />", "/" %></div>
  <div class="login">
        <% if @cur_member %>
                <b><%= @cur_member.full_name %></b> (<%= @cur_member.username %>)<br/>
                <%= link_to "Profile", edit_member_path(@cur_member) %> <%= link_to("Logout", logout_path) %>
        <% else %>
                <br/>(not logged in) 
        <% end %>
</div>
        <div id="menu">
                <%= link_to_if(@cur_member.has_permission?("Events"), "event", "list") %>
                <%= link_to_if(@cur_member.has_permission?("Email"), "email", "list") %>
                <%= link_to_if(@cur_member.has_permission?("Calendar"), "event", "calendar") %>
                <%= link_to_if(@cur_member.has_permission?("Finance"), "account", "list") %>
                <%= link_to_if(@cur_member.has_permission?("Phone List"), "member", "list") %>
                <% if is_iphone_request? %>
                  <%= @cur_member.has_permission(@cur_member.has_permission?("iPhone View"), "event", "iphone") %>
                <% end %>
                <% admin = @cur_member.has_permission? ["organizations/index", "members/list", "equipment/tree", "role/list", "permission/list", "bugs/index"] %>
      <%  if admin %>  
        <%= link_to_function("Tracker Admin", nil, :id => "more_link") do |page|
        page.visual_effect(:toggle_blind, "submenu", :duration => 0.1) 
            end %>
      <% end %>
        </div>
      <% if admin %>
        <div id="submenu" style="display: none;">
              <%= link_to_if(@cur_member.has_permission?("Organizations"), "organizations", "index") %>
              <%= link_to_if @cur_member.has_permission?("Members"), "member", "list"  %>  
              <%= link_to_if @cur_member.has_permission?("Locations"), "location", "list" %>
              <%= link_to_if(@cur_member.has_permission?("Equipment"), "equipment", "tree" ) %>
              <%= link_to_if(@cur_member.has_permission?("Roles"), "role", "list" ) %>
              <%= link_to_if(@cur_member.has_permission?("Permissions"), "permission", "list" ) %>
              <%= link_to_if(@cur_member.has_permission?("Attachments"), "attachments", "index" ) %>
              <%= link_to_if(@cur_member.has_permission?("Bugs (Bug.count_open)"), "bugs", "index" )  %>
          </div>
        <% end %>

<% if flash[:notice] %><div class="notice"><%= flash[:notice] %></div><% end %>
<% if flash[:error] %> <div class="error"> <%= flash[:error] %> </div><% end %>


<%# the event controller is special %>
<% unless controller.controller_name == 'event' %>
<div id="container">
  <%= yield %>
</div>
<%- else -%>
<%= yield %>
<%- end -%>
</body>
</html>
